Feature: 只读运行观测面板
  测试工程师可以在本机浏览器查看 Auto-Test 的运行列表与状态,观测面不写任何文件、不暴露任何秘密。

  Rule: 观测面是只读投影

    Scenario: 启动观测面板并查看运行列表
      Given 平台运行根目录下存在多个已完成或进行中的运行
      When 测试工程师启动观测面板并获得本机回环地址
      Then 浏览器打开该地址显示运行列表
      And 每个运行显示目录名、状态徽标、最终结果、开始与更新时间
      And 列表按更新时间从新到旧排序

    Scenario: 没有运行记录时显示空态
      Given 平台运行根目录为空或不存在
      When 测试工程师打开观测面板
      Then 页面显示"还没有任何运行记录"而非错误

    Scenario: 损坏的状态文件不阻断列表
      Given 某运行目录中的状态文件不是合法 JSON 或不是当前版本
      When 测试工程师打开运行列表
      Then 该运行以"无法读取"状态出现
      And 其余运行正常列出

  Rule: 观测面永远不越过隐私边界

    Scenario: 私有目录内容不可访问
      Given 某运行目录包含 .agent-private 下的秘密材料
      When 观测面板的任何 API 被请求
      Then 响应不包含 .agent-private 的任何内容
      And 不存在返回原始工作簿或秘密的路径

  Rule: 观测与执行分离

    Scenario: 观测面板不干扰运行
      Given 一个 Auto-Test 运行正在进行
      When 测试工程师同时启动观测面板
      Then 运行行为不变,观测面板只读取运行产物
      And 关闭观测面板不影响运行

  Rule: 非回环暴露必须携带访问令牌

    Scenario: 局域网内查看观测面板
      Given 测试工程师用 --host 绑定非回环地址启动观测面板
      And 面板自动生成(或采用 --token 指定的)访问令牌
      When 测试工程师在局域网内浏览器打开打印的完整 URL(含令牌)
      Then 页面正常加载且实时事件与证据链接自动携带令牌
      And 任何不带令牌或令牌错误的请求得到 401
      And 默认的回环启动行为保持不变(无需令牌)
